# ─── Your Own World — environment variables ─────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# .env.local is gitignored. Never commit real credentials.
#
# VITE_ prefix  → exposed to the browser bundle (public, safe for keys
#                  that are designed to be public, like anon keys)
# No prefix     → server-side only, used by Vercel API routes (api/*.js)
#                  Set these in the Vercel dashboard under Settings → Environment Variables


# ── Supabase ─────────────────────────────────────────────────────────────────
# Project dashboard → Settings → API

# Public anon key — safe to expose, access is controlled by Row Level Security
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Also needed server-side (Vercel API routes verify auth tokens against Supabase)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Service role key — NEVER expose to browser. Used by stripe-webhook and
# create-customer-portal to write subscription data back to user app_metadata.
# Project dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here


# ── Stripe ───────────────────────────────────────────────────────────────────
# dashboard.stripe.com → Developers → API keys  /  Products → Prices

# Secret key — server-side only, never put in VITE_ vars
STRIPE_SECRET_KEY=sk_test_...

# One price ID per plan — api/create-checkout-session.js looks these up by
# plan key (hosting_renewal is an alias for maintenance, same price).
STRIPE_PRICE_ID_PREMIUM_MONTHLY=price_...
STRIPE_PRICE_ID_PREMIUM_PLUS_LIFETIME=price_...
STRIPE_PRICE_ID_FOUNDER=price_...
STRIPE_PRICE_ID_MAINTENANCE=price_...

# Legacy fallback for premium_monthly only, used if
# STRIPE_PRICE_ID_PREMIUM_MONTHLY is unset. New setups should set that
# instead; kept for accounts configured before the per-plan vars existed.
STRIPE_PRICE_ID=price_...

# Webhook signing secret — Stripe dashboard → Webhooks → your endpoint → Signing secret
# For local testing with the Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe-webhook
STRIPE_WEBHOOK_SECRET=whsec_...


# ── App URL ──────────────────────────────────────────────────────────────────
# Used by Stripe for success/cancel redirect URLs.
# In production this is your Vercel deployment URL.
# Locally: http://localhost:3000 when using `vercel dev`, or http://localhost:5173 for `npm run dev`
SITE_URL=http://localhost:3000


# ── API endpoint URLs (browser-accessible) ───────────────────────────────────
# In production these are relative paths — Vercel routes /api/* automatically.
# Change to full URLs only if running the API on a different domain.
VITE_CREATE_CHECKOUT_SESSION_URL=/api/create-checkout-session
VITE_CUSTOMER_PORTAL_URL=/api/create-customer-portal
VITE_REGISTER_PAID_INTEREST_URL=/api/register-paid-interest
VITE_GET_DOWNLOAD_LINKS_URL=/api/get-download-links
VITE_GET_FOUNDER_SLOTS_URL=/api/get-founder-slots

# Base URL the desktop (Tauri) build calls for these same API routes, since
# the desktop app has no same-origin Vercel deployment to route /api/* through
# relatively. Web builds ignore this. Optional — falls back to the production
# site when running as the desktop app, and to '' (relative) otherwise.
# VITE_DESKTOP_API_BASE_URL=https://www.yourownworld.co.uk

# Used by /api/submit-feedback and /api/register-paid-interest to send owner emails.
FEEDBACK_EMAIL=yourownworld.admin@gmail.com
FEEDBACK_EMAIL_PASSWORD=your-gmail-app-password


# ── AI settings sync (server-side only) ──────────────────────────────────────
# Encrypts a signed-in user's AI provider settings (api/ai-settings.js) at
# rest so they can sync across devices. AI_SETTINGS_ENCRYPTION_KEY is
# preferred (a 32-byte key, hex or base64); AI_SETTINGS_SECRET is an older
# alias accepted as a fallback and deterministically hashed into a key if
# it isn't already 32 bytes. Set exactly one.
AI_SETTINGS_ENCRYPTION_KEY=generate-a-32-byte-hex-or-base64-key

# Optional — durable per-user rate limit on api/ai-proxy.js requests.
# Sane defaults (30 requests / 10 minutes) apply if unset.
# AI_PROXY_RATE_LIMIT_MAX=30
# AI_PROXY_RATE_LIMIT_WINDOW_MINUTES=10


# ── Desktop app (Tauri) ───────────────────────────────────────────────────────
# api/desktop-devices.js device-activation registry and api/get-download-links.js.

# Signs the per-device entitlement record api/desktop-devices.js issues, so
# the desktop app can verify it wasn't tampered with offline. Generate any
# long random string.
ENTITLEMENT_SIGNING_SECRET=generate-a-long-random-string

# Optional — max simultaneously active devices per account. Defaults to 3.
# DESKTOP_DEVICE_CAP=3

# Installer URLs returned by api/get-download-links.js after entitlement is
# verified server-side — kept out of the client bundle entirely.
DESKTOP_DOWNLOAD_URL_MACOS=https://...
DESKTOP_DOWNLOAD_URL_WINDOWS=https://...

# Optional — version string shown alongside the download links.
# DESKTOP_APP_VERSION=1.0.0


# ── AI providers ─────────────────────────────────────────────────────────────
# At least one of these is required for the AI assistant to work.

# OpenRouter (recommended default — free tier available, access to many models)
# openrouter.ai → Keys
VITE_OPENROUTER_API_KEY=sk-or-...

# Google AI Studio (optional — for Gemini / Gemma models directly)
# aistudio.google.com → Get API key
# VITE_GOOGLE_AI_API_KEY=AIza...

# Anthropic (optional — for Claude models directly)
# console.anthropic.com → API Keys
# VITE_ANTHROPIC_API_KEY=sk-ant-...


# ── Email / re-engagement (server-side only) ─────────────────────────────────
# Required for the day-1/3/7 "come back" email sweep (api/send-reengagement-emails.js,
# supabase/functions/send-reengagement-email) and the shared password-reset/
# welcome-email sender (supabase/functions/send-reset-email, send-welcome-email).
# All of these now fail closed (500/401) if their required secret is unset —
# see docs/YOW_CODE_AUDIT_2026-09-01.md finding P0-03.

# resend.com → API Keys. Used by all three transactional-email Edge Functions.
RESEND_API_KEY=re_...

# Vercel Cron sends this as `Authorization: Bearer $CRON_SECRET` automatically
# when set as a Vercel project env var (Settings → Cron Jobs). Generate any
# long random string; must also be set as a Supabase secret so
# send-reengagement-email/index.ts's own trusted-caller check matches it.
CRON_SECRET=generate-a-long-random-string

# Signs the one-click re-engagement unsubscribe link
# (api/reengagement-unsubscribe.js, supabase/functions/send-reengagement-email)
# so a bare user id in a GET URL isn't enough on its own. Generate any long
# random string; must be set identically as both a Vercel and a Supabase
# secret, since the two runtimes verify against the same value.
REENGAGEMENT_UNSUBSCRIBE_SECRET=generate-a-long-random-string

# Optional — durable per-email rate limit on password-reset requests
# (supabase/functions/send-reset-email). Both have sane defaults if unset.
# PASSWORD_RESET_RATE_LIMIT_MAX=5
# PASSWORD_RESET_RATE_LIMIT_WINDOW_MINUTES=15


# ── Dev helpers ───────────────────────────────────────────────────────────────
# Auto-fills the login form in development mode only.
# Remove or leave blank in production.
VITE_DEV_EMAIL=you@example.com
VITE_DEV_PASSWORD=your-password

# Replaces Supabase/AI calls with local stubs — no real backend needed.
# playwright.config.mjs sets this inline for npm run qa:smoke; set it
# yourself only if you want the same offline stub in npm run dev.
# VITE_OFFLINE_MODE=true
