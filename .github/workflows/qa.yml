name: QA

# claude/dev-team-member-skills-ts02xj is a long-lived active-development fork
# (the dev-team-member-skills work, not yet merged to main) that has been
# accumulating PRs with no CI signal at all, since this workflow previously
# only triggered against main. Remove that branch from the lists below once
# it merges back into main.
on:
  push:
    branches:
      - main
      - claude/dev-team-member-skills-ts02xj
  pull_request:
    branches:
      - main
      - claude/dev-team-member-skills-ts02xj
  workflow_dispatch:
  schedule:
    - cron: '17 3 * * *'

concurrency:
  group: qa-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  static-qa:
    name: Static QA
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run static QA
        run: npm run qa

  smoke:
    name: Smoke / ${{ matrix.name }}
    needs: static-qa
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PLAYWRIGHT_BROWSERS_PATH: /tmp/yow-ms-playwright
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: launch
            spec: tests/e2e/launch-smoke.spec.js
          - name: project-types
            spec: tests/e2e/project-types.spec.js
          - name: export-formats
            spec: tests/e2e/export-formats.spec.js
          - name: responsive
            spec: tests/e2e/responsive-smoke.spec.js
          - name: autosave
            spec: tests/e2e/autosave.spec.js
          - name: manuscript-structure
            spec: tests/e2e/manuscript-structure.spec.js
          - name: worldbuilding
            spec: tests/e2e/worldbuilding.spec.js
          - name: dashboard
            spec: tests/e2e/dashboard.spec.js
          - name: comic-planner
            spec: tests/e2e/comic-planner.spec.js
          - name: data-safety
            spec: tests/e2e/data-safety.spec.js
          - name: url-persistence
            spec: tests/e2e/url-persistence.spec.js
          - name: account-isolation
            spec: tests/e2e/account-isolation.spec.js
          - name: project-type-labels
            spec: tests/e2e/project-type-labels.spec.js
          - name: manuscript-reference-panel
            spec: tests/e2e/manuscript-reference-panel.spec.js
          - name: new-account-onboarding
            spec: tests/e2e/new-account-onboarding.spec.js
          - name: pricing-page
            spec: tests/e2e/pricing-page.spec.js
          - name: unconfigured-ai-account
            spec: tests/e2e/unconfigured-ai-account.spec.js

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run ${{ matrix.name }} smoke tests
        run: npx playwright test --config playwright.config.mjs ${{ matrix.spec }}

      - name: Upload smoke test artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-${{ matrix.name }}-artifacts
          path: |
            test-results/
            playwright-report/
          if-no-files-found: ignore
          retention-days: 7
